import React, { useState } from 'react'; import { MapContainer, TileLayer, Marker, Popup } from 'react-leaflet'; import 'leaflet/dist/leaflet.css'; import { getByPostal } from '../services/listings'; export default function Search(){ const [postal,setPostal]=useState(''); const [listings,setListings]=useState([]); const doSearch=async ()=>{ try{ const r = await getByPostal(postal); setListings(r); }catch(e){console.error(e);} }; return (<div><h2>Search</h2><div style={{display:'flex',gap:12}}><input value={postal} onChange={e=>setPostal(e.target.value)} placeholder='ZIP code' /><button className='btn primary' onClick={doSearch}>Search</button></div>{listings.length>0 && (<div style={{marginTop:12}}><MapContainer center={[listings[0].location.address.coordinate.lat, listings[0].location.address.coordinate.lon]} zoom={12} style={{height:400}}><TileLayer url='https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png' attribution='&copy; OpenStreetMap contributors'/> {listings.map((l,idx)=>{ const c=l.location.address.coordinate; if(!c) return null; return (<Marker key={idx} position={[c.lat,c.lon]}><Popup><strong>{l.location.address.line}</strong><br/>Price: ${l.price || l.list_price}</Popup></Marker>); })}</MapContainer></div>)}</div>);}